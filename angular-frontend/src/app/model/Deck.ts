export class Deck
{
  main: number[];
  side: number[];
  ex: number[];
  constructor(main: number[], side: number[], ex: number[]) {
    this.main = main;
    this.side = side;
    this.ex = ex;
  }
}
